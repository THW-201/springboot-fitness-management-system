<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.mapper.SocialCommentMapper">

    <resultMap id="SocialCommentMap" type="com.fitness.entity.SocialComment">
        <id column="id" property="id" />
        <result column="post_id" property="postId" />
        <result column="user_id" property="userId" />
        <result column="content" property="content" />
        <result column="created_at" property="createdAt" />
        <result column="userName" property="userName" />
        <result column="user_avatar" property="userAvatar" />
    </resultMap>

    <select id="getCommentsByPostId" resultMap="SocialCommentMap">
        SELECT
            c.*,
            COALESCE(u.real_name, u.username) as userName,
            u.avatar_url as user_avatar
        FROM social_comment c
                 LEFT JOIN users u ON c.user_id = u.id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>

    <select id="getCommentCountByPostId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM social_comment WHERE post_id = #{postId}
    </select>

    <!-- 新增：根据评论ID查询评论，关联用户信息 -->
    <select id="selectCommentWithUserById" resultMap="SocialCommentMap">
        SELECT
            c.*,
            COALESCE(u.real_name, u.username) as userName,
            u.avatar_url as user_avatar
        FROM social_comment c
                 LEFT JOIN users u ON c.user_id = u.id
        WHERE c.id = #{id}
    </select>

    <!-- 新增：获取评论列表（管理员），关联用户信息 -->
    <select id="getAdminComments" resultMap="SocialCommentMap">
        SELECT
            c.*,
            COALESCE(u.real_name, u.username) as userName,
            u.avatar_url as user_avatar
        FROM social_comment c
                 LEFT JOIN users u ON c.user_id = u.id
        ORDER BY c.created_at DESC
            LIMIT #{size} OFFSET #{current}
    </select>

    <!-- 新增：获取评论总数（管理员） -->
    <select id="getAdminCommentCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM social_comment
    </select>

</mapper>